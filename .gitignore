# Simulation output — regenerated on every run, never committed
runs/

# Secrets — never commit. .env.example is the safe, checked-in template.
.env
.env.*
!.env.example

# Rendered maps — rebuild with `python3 render_world.py`, never committed
*.html

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.venv-*/
venv/
env/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
